node:
  verbose: true
  name: mynode
  ipv4: 0.0.0.0
  port: 8100
  data-dir: /var/dreg/data/
  upstream:
    address: https://ish-ar.io
    insecure: true
  proxy:
    regex: ".*distribution.*"
  scheduler:
    address: http://scheduler:8000
  