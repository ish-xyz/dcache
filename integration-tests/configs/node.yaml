node:
  verbose: false
  port: 8100
  dataDir: /var/dcache/data
  upstream:
    address: https://ish-ar.io
    insecure: true
  proxy:
    regex: ".*distribution.*"
  scheduler:
    address: http://scheduler:8000
  gc:
    maxAtimeAge: 3m
    interval: 5s
    maxDiskUsage: 100G
    minDiskFree: 20G
